#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../htmling/bin/htmling" "$@"
  ret=$?
else 
  node  "$basedir/../htmling/bin/htmling" "$@"
  ret=$?
fi
exit $ret
